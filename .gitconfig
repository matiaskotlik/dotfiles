# This is Git's per-user configuration file.
[user]
# Please adapt and uncomment the following lines:
	name = Matias Kotlik
	email = qmatias@users.noreply.github.com
[alias]
	co = checkout
	br = branch
	ci = commit
	st = status
	unstage = reset HEAD --
	ignore = "!gi() { curl -sL https://www.toptal.com/developers/gitignore/api/$@ ;}; gi"
